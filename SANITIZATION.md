# Sanitized snapshot

History-free snapshot of `AIS-Cloosphere` (HEAD), prepared for read-only use with
external tools. **Not** a working development clone.

- **No git history.** Built from a single `git archive HEAD`, then re-initialized
  as a fresh repo with one commit.
- **Removed files:**
  - `.env`
  - `scripts/.license_keys/private_key.pem`
- **Redacted occurrences:** 22 (secrets replaced with `<REDACTED>`).
- **Aliased occurrences:** 280 (sensitive terms rewritten to neutral aliases).
- **`.gitignore` hardened** so secret files cannot be re-tracked.

Regenerated by the `sanitize-mirror` skill. This is still proprietary source —
sanitizing secrets does not change ownership or licensing.
